let month = prompt(`Введите номер месяца`);
function monthNumber(month) {
    if (month === `1`) {
        console.log(`Зима`);
    } 
    else if (month === `2`) {
        console.log(`Зима`);
    }
    else if (month === `3`) {
        console.log(`Весна`);
    }
    else if (month === `4`) {
        console.log(`Весна`);
    }
    else if (month === `5`) {
        console.log(`Весна`);
    }
    else if (month === `6`) {
        console.log(`Лето`);
    }
    else if (month === `7`) {
        console.log(`Лето`);
    }
    else if (month === `8`) {
        console.log(`Лето`);
    }
    else if (month === `9`) {
        console.log(`Осень`);
    }
    else if (month === `10`) {
        console.log(`Осень`);
    }
    else if (month === `11`) {
        console.log(`Осень`);
    }
    else if (month === `12`) {
        console.log(`Зима`);
    }
    else {
        console.log(`Прочитай задание еще раз`);
    }
}